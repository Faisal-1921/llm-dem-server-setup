# 跨机器共享材料

本目录随公共仓 `Faisal-1921/llm-dem-server-setup` 分发。不是业务代码，也不是 V1.1 运行入口。

其他机器上的 Agent：**先 `git pull --ff-only`，再取下表文件。** 不要从 Windows 本地盘 `E:\Mike_项目材料整理_...` 去拷。

| 文件 | 仓内路径 | 上传 |
|---|---|---|
| 周禹轩-汇报3.pptx | `shared/briefings/周禹轩-汇报3.pptx` | 2026-09-10，`win-desktop` |

拉取（无需 GitHub 登录）：

```bash
git clone https://github.com/Faisal-1921/llm-dem-server-setup.git
# 或已有 clone：
git pull --ff-only
```

Windows 建议落盘：`%USERPROFILE%\Documents\LEM-DEM-public`。
